---
phase: 02-run-input-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - streamlit_app/runtime/session_state.py
  - streamlit_app/services/upload_staging.py
  - tests/test_input_staging.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
user_setup: []
must_haves:
  truths:
    - Uploaded sales, stock, and SKU/live files are kept only for the active run instead of becoming persistent history.
    - Re-uploading sales, stock, or SKU/live in the same run keeps only the current file for that slot.
  artifacts:
    - streamlit_app/services/upload_staging.py
    - streamlit_app/runtime/session_state.py
    - tests/test_input_staging.py
  key_links:
    - Session bootstrap and upload-step state resolve to the same staged input registry for sales, stock, and SKU/live slots.
    - The upload staging helpers write only into `workspace_input_dir` under the active session workspace.
---

<objective>
Create the reusable upload staging contract for the three run-scoped input files.

Purpose: Give the app a real session-local place to stage uploaded sales, stock, and SKU/live files before validation and later V5 execution.
Output: Upload slot metadata helpers, deterministic staging paths, replace-in-place behavior, and focused unit coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-run-input-workflow/02-CONTEXT.md
@.planning/phases/02-run-input-workflow/02-RESEARCH.md
@streamlit_app/runtime/session_state.py
@streamlit_app/runtime/temp_workspace.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upload slot contracts and canonical session staging paths</name>
  <files>streamlit_app/services/upload_staging.py, streamlit_app/runtime/session_state.py, tests/test_input_staging.py</files>
  <action>Create the Phase 2 upload-slot contract for sales, stock, and SKU/live files. Define deterministic staged paths inside the existing `workspace_input_dir`, plus session-state structures that let the upload step track the current file for each slot. Add initial unit tests in the same task so the session-local staging behavior is proven immediately.</action>
  <verify>
    <automated>pytest -q tests/test_input_staging.py -k session</automated>
  </verify>
  <done>The app has a reusable staged-input registry for the three required files, and the active session workspace is the only place those files are staged.</done>
</task>

<task type="auto">
  <name>Task 2: Implement replace-in-place upload staging and metadata extraction</name>
  <files>streamlit_app/services/upload_staging.py, tests/test_input_staging.py</files>
  <action>Implement the rule that a new upload for the same slot replaces the current staged file in the active session instead of creating version history. Extract the compact metadata Phase 2 needs after upload, such as source filename, byte size, and staged path, so later validation and UI layers can reuse it without reparsing the raw widget object.</action>
  <verify>
    <automated>pytest -q tests/test_input_staging.py -k replace</automated>
  </verify>
  <done>Re-uploading a slot overwrites the active staged file and refreshes its metadata atomically within the session workspace contract.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the staging contract with focused unit coverage</name>
  <files>tests/test_input_staging.py</files>
  <action>Expand the staging tests so they cover all three input slots, deterministic path behavior, replacement semantics, and the rule that no staging escapes the managed session input directory. Keep the test file filesystem-local and fast so it can remain the wave-1 feedback loop for later phases.</action>
  <verify>
    <automated>pytest -q tests/test_input_staging.py</automated>
  </verify>
  <done>Fast unit coverage protects the upload staging contract and makes later validation and UI work safe to build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_input_staging.py` passes
- [ ] Each required file slot stages only into the active session input directory
- [ ] Re-uploads replace the current staged file instead of accumulating session history
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has reusable run-input staging primitives for sales, stock, and SKU/live uploads
- Phase 2 can build validation and UI layers without inventing a second storage contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-run-input-workflow/02-01-SUMMARY.md`
</output>
