---
phase: 02-run-input-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - streamlit_app/services/input_validation.py
  - tests/test_input_validation.py
autonomous: true
requirements:
  - DATA-05
user_setup: []
must_haves:
  truths:
    - Uploaded files are checked against the frozen V5 input contract before the operator moves on.
    - Critical validation failures block progress, while non-critical findings remain visible as warnings.
  artifacts:
    - streamlit_app/services/input_validation.py
    - tests/test_input_validation.py
  key_links:
    - The validation service consumes staged slot metadata from the upload staging contract and returns both blocking errors and non-blocking warnings.
    - Validation summaries expose row count and date/month hints for the UI without requiring full data previews.
---

<objective>
Implement the Phase 2 validation service for uploaded run-scoped files.

Purpose: Encode the frozen V5 input expectations into reusable validation logic before the UI exposes the upload workflow to operators.
Output: A slot-aware validation service, warning classification rules, and focused tests for critical and non-critical outcomes.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-run-input-workflow/02-CONTEXT.md
@.planning/phases/02-run-input-workflow/02-RESEARCH.md
@.planning/phases/02-run-input-workflow/02-01-SUMMARY.md
@v5_daily_oos_opportunity/data_loader_v5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode critical validation rules from the frozen V5 input contract</name>
  <files>streamlit_app/services/input_validation.py, tests/test_input_validation.py</files>
  <action>Create a reusable validation service that checks each staged input slot against the current V5 contract: supported format posture, readable parse, and required columns. Keep the rules slot-aware so sales, stock, and SKU/live use the right expectations, and add the initial critical-path tests in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_input_validation.py -k critical</automated>
  </verify>
  <done>The app can classify unreadable files, unsupported formats, and missing required columns as blocking failures for each upload slot.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-blocking warnings and compact upload summaries</name>
  <files>streamlit_app/services/input_validation.py, tests/test_input_validation.py</files>
  <action>Extend the validation service so it also returns visible warnings and a compact upload summary suitable for the UI. Focus on Phase 2 scope: row counts, empty-or-near-empty datasets, and key date/month hints when they can be extracted cheaply. Do not drift into business-logic QA that belongs in later phases.</action>
  <verify>
    <automated>pytest -q tests/test_input_validation.py -k warnings</automated>
  </verify>
  <done>Validation returns both blocking errors and non-blocking warnings plus the summary metadata needed by the upload step.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the validation contract with focused unit coverage</name>
  <files>tests/test_input_validation.py</files>
  <action>Expand unit coverage so it protects the supported formats, column requirements, blocking-vs-warning split, and compact summary extraction for all three input slots. Keep the suite fixture-driven and fast so the validation layer remains a cheap safety net during later upload UI work.</action>
  <verify>
    <automated>pytest -q tests/test_input_validation.py</automated>
  </verify>
  <done>Phase 2 has a reliable validation contract that mirrors the frozen V5 input expectations without requiring the actual V5 run path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_input_validation.py` passes
- [ ] Critical validation failures block unreadable or schema-incompatible files
- [ ] Non-critical warnings and compact summaries are available for the UI layer
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Validation behavior matches the Phase 2 decisions and the current V5 file contract
- Later UI work can reuse one validation service instead of duplicating rules in widgets
</success_criteria>

<output>
After completion, create `.planning/phases/02-run-input-workflow/02-02-SUMMARY.md`
</output>
