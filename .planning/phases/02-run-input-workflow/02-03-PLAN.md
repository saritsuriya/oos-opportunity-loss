---
phase: 02-run-input-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - streamlit_app/services/v5_boundary.py
  - streamlit_app/ui/upload_inputs.py
  - streamlit_app/ui/wizard.py
  - tests/test_upload_step_ui.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
user_setup: []
must_haves:
  truths:
    - Operators can stage the required sales, stock, and SKU/live files from one browser screen in the upload step.
    - The upload step shows clear blocking errors, visible warnings, useful file summaries, and the bundled site-mapping status without requiring a site-map upload.
  artifacts:
    - streamlit_app/ui/upload_inputs.py
    - streamlit_app/ui/wizard.py
    - streamlit_app/services/v5_boundary.py
    - tests/test_upload_step_ui.py
  key_links:
    - The wizard routes the `upload-inputs` step into the dedicated upload UI module instead of leaving a placeholder.
    - The upload UI uses the shared staging and validation services to render per-slot status and readiness.
    - The upload UI reads bundled site-mapping information from the V5 boundary rather than a second config path.
---

<objective>
Wire the upload staging and validation contracts into the real Streamlit upload step.

Purpose: Replace the Phase 1 placeholder with the actual Phase 2 operator workflow for uploading, validating, and summarizing the required run-scoped inputs.
Output: A three-card upload UI, bundled site-mapping visibility, readiness state, and Streamlit AppTest coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-run-input-workflow/02-CONTEXT.md
@.planning/phases/02-run-input-workflow/02-RESEARCH.md
@.planning/phases/02-run-input-workflow/02-01-SUMMARY.md
@.planning/phases/02-run-input-workflow/02-02-SUMMARY.md
@streamlit_app/ui/wizard.py
@streamlit_app/services/v5_boundary.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the three-card upload step and readiness state in the wizard shell</name>
  <files>streamlit_app/ui/upload_inputs.py, streamlit_app/ui/wizard.py, tests/test_upload_step_ui.py</files>
  <action>Replace the Phase 1 placeholder in the `upload-inputs` step with a dedicated upload UI module. Render the three required file cards on one screen, connect them to the shared staging and validation contracts, and surface a clear overall readiness state for the step. Add the initial AppTest coverage in the same task so the layout and per-card state are verified immediately.</action>
  <verify>
    <automated>pytest -q tests/test_upload_step_ui.py -k layout</automated>
  </verify>
  <done>The upload step is no longer a placeholder; operators can see and interact with all three required input cards inside the wizard shell.</done>
</task>

<task type="auto">
  <name>Task 2: Surface bundled site-mapping status and useful upload summaries</name>
  <files>streamlit_app/services/v5_boundary.py, streamlit_app/ui/upload_inputs.py, tests/test_upload_step_ui.py</files>
  <action>Extend the upload step so it shows bundled site-mapping information in read-only form and renders the useful per-file summaries chosen during context capture. Reuse the V5 boundary as the source of truth for site-mapping context, and make sure blocking errors versus warnings are visually distinct without adding a heavy data preview experience.</action>
  <verify>
    <automated>pytest -q tests/test_upload_step_ui.py -k site_map</automated>
  </verify>
  <done>The upload step shows bundled site-mapping status, compact file summaries, and clear blocking/warning feedback without requiring a site-map upload.</done>
</task>

<task type="auto">
  <name>Task 3: Add AppTest coverage for upload readiness and validation states</name>
  <files>tests/test_upload_step_ui.py</files>
  <action>Expand Streamlit AppTest coverage so it protects the upload layout, bundled site-mapping visibility, readiness state, and the surfaced critical/warning feedback. Keep the test fixtures lightweight and centered on Phase 2 behaviors, not later V5 execution or export concerns.</action>
  <verify>
    <automated>pytest -q tests/test_upload_step_ui.py</automated>
  </verify>
  <done>Phase 2 has browser-level coverage for the upload step and can hand off a trustworthy staged-input workflow to Phase 3.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_upload_step_ui.py` passes
- [ ] The upload step shows three required input cards on one screen
- [ ] Blocking errors, warnings, useful summaries, and bundled site-mapping status all surface in the wizard UI
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The browser workflow now collects the run-scoped inputs instead of showing a placeholder
- Phase 3 receives a real staged-input workflow and readiness signal from the existing wizard shell
</success_criteria>

<output>
After completion, create `.planning/phases/02-run-input-workflow/02-03-SUMMARY.md`
</output>
