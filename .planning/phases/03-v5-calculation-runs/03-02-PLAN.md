---
phase: 03-v5-calculation-runs
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - streamlit_app/runtime/session_state.py
  - streamlit_app/services/run_workflow.py
  - tests/test_run_workflow.py
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
user_setup: []
must_haves:
  truths:
    - The app tracks the run-v5 flow with session-visible idle, running, succeeded, and failed states instead of relying on transient widget behavior.
    - The operator can rerun inside the same session with the current staged files unless they choose to replace those files in the upload step first.
  artifacts:
    - streamlit_app/runtime/session_state.py
    - streamlit_app/services/run_workflow.py
    - tests/test_run_workflow.py
  key_links:
    - The run workflow consumes Phase 2 upload readiness and the extracted 03-01 run service to decide whether execution can start.
    - Session state exposes one structured run status payload that both the run step and later result-review phases can read.
---

<objective>
Implement session-local run orchestration, error handling, and rerun behavior.

Purpose: Turn the raw frozen V5 run service into a browser-safe workflow that the wizard can trust across reruns and failures.
Output: Run-state helpers, orchestrated success/failure payloads, rerun behavior, and focused unit coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-v5-calculation-runs/03-CONTEXT.md
@.planning/phases/03-v5-calculation-runs/03-RESEARCH.md
@.planning/phases/03-v5-calculation-runs/03-01-SUMMARY.md
@streamlit_app/runtime/session_state.py
@streamlit_app/services/run_execution.py
@streamlit_app/ui/upload_inputs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the session run-status contract and execution preconditions</name>
  <files>streamlit_app/runtime/session_state.py, streamlit_app/services/run_workflow.py, tests/test_run_workflow.py</files>
  <action>Create the Phase 3 session-state contract for the run step, including selected month, suggested month, idle/running/succeeded/failed status, and the precondition checks that guard execution. Add initial unit tests in the same task so the status transitions and pre-run gating are proven immediately.</action>
  <verify>
    <automated>pytest -q tests/test_run_workflow.py -k status</automated>
  </verify>
  <done>The app has a stable run-status payload in session state and a clear rule for when the frozen V5 run can start.</done>
</task>

<task type="auto">
  <name>Task 2: Implement rerun-safe orchestration and structured failure handling</name>
  <files>streamlit_app/services/run_workflow.py, tests/test_run_workflow.py</files>
  <action>Wrap the extracted 03-01 run service in a workflow helper that starts the run, captures success or failure details, and supports rerun with the current staged files in the same session. Keep failure handling structured so the UI can explain what happened without dumping raw tracebacks, and add focused rerun/error tests in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_run_workflow.py -k rerun</automated>
  </verify>
  <done>Phase 3 can rerun deterministically inside one session and expose structured error or success payloads for the run step.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the orchestration contract with focused unit coverage</name>
  <files>tests/test_run_workflow.py</files>
  <action>Expand unit coverage so it protects pre-run gating, status transitions, month override handling, rerun semantics, and failure capture for the session-local run workflow. Keep the suite fast and service-level so the later UI plan can build on it safely.</action>
  <verify>
    <automated>pytest -q tests/test_run_workflow.py</automated>
  </verify>
  <done>Fast unit coverage protects the run-orchestration contract and keeps the browser workflow reliable across reruns and failures.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_run_workflow.py` passes
- [ ] Run status is preserved in session state across reruns
- [ ] Rerun uses the current staged files instead of forcing reupload
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has one session-local run workflow that reports idle/running/succeeded/failed states clearly
- The later run-step UI can read one orchestration contract instead of managing execution state ad hoc
</success_criteria>

<output>
After completion, create `.planning/phases/03-v5-calculation-runs/03-02-SUMMARY.md`
</output>
