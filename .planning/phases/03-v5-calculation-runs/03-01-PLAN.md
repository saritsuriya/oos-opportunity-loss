---
phase: 03-v5-calculation-runs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - streamlit_app/services/v5_boundary.py
  - streamlit_app/services/run_execution.py
  - tests/test_run_execution.py
autonomous: true
requirements:
  - RUN-01
  - RUN-02
user_setup: []
must_haves:
  truths:
    - The browser can derive a concrete frozen V5 run request from the staged session files and the selected evaluation month without shelling out to the CLI.
    - A successful frozen V5 run writes its workbook and CSV artifacts only into the active session output workspace and returns a structured result the app can reuse.
  artifacts:
    - streamlit_app/services/v5_boundary.py
    - streamlit_app/services/run_execution.py
    - tests/test_run_execution.py
  key_links:
    - The extracted run service builds `InputPaths` and `ModelConfig` from Phase 2 staged files plus the selected evaluation month.
    - The run service invokes the frozen V5 Python modules directly and records the generated workbook and CSV outputs under `workspace_output_dir`.
---

<objective>
Extract the reusable frozen V5 run service from the current script flow.

Purpose: Give the app a direct, testable execution seam for Phase 3 without relying on the CLI entrypoint or subprocess calls.
Output: Suggested-month helpers, a structured run request/result contract, direct V5 module execution, and focused unit coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-v5-calculation-runs/03-CONTEXT.md
@.planning/phases/03-v5-calculation-runs/03-RESEARCH.md
@streamlit_app/services/v5_boundary.py
@v5_daily_oos_opportunity/main.py
@v5_daily_oos_opportunity/data_loader_v5.py
@v5_daily_oos_opportunity/analyzer_v5.py
@v5_daily_oos_opportunity/reporter_v5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the run-request contract and suggested-month helpers</name>
  <files>streamlit_app/services/v5_boundary.py, streamlit_app/services/run_execution.py, tests/test_run_execution.py</files>
  <action>Create the Phase 3 run-request contract that translates the staged sales, stock, and SKU/live files plus bundled site mapping into a concrete frozen V5 request. Add a helper that suggests the evaluation month from the staged stock file while still allowing operator override later, and cover that behavior with initial unit tests in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_run_execution.py -k month</automated>
  </verify>
  <done>The app can derive a browser-side V5 run request and a suggested month from the current staged session inputs without going through the CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the frozen V5 modules directly and return a structured run result</name>
  <files>streamlit_app/services/v5_boundary.py, streamlit_app/services/run_execution.py, tests/test_run_execution.py</files>
  <action>Implement the direct execution path that uses the frozen V5 loader, analyzer, and reporter modules from Python code. Write results only into the active session output workspace, return a structured success payload with artifact paths and core metadata, and add focused execution-path tests in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_run_execution.py -k execute</automated>
  </verify>
  <done>A successful Phase 3 run produces the workbook and CSV artifact set inside the session output directory and returns a structured result the browser can display.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the run-service contract with focused unit coverage</name>
  <files>tests/test_run_execution.py</files>
  <action>Expand unit coverage so it protects month suggestion, path construction, successful execution, and structured failure handling for the extracted run service. Keep the suite fixture-driven and fast so later orchestration and UI work can depend on it safely.</action>
  <verify>
    <automated>pytest -q tests/test_run_execution.py</automated>
  </verify>
  <done>Fast unit coverage protects the extracted frozen V5 run seam and the structured success/failure result contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_run_execution.py` passes
- [ ] The run request is built from staged session inputs plus the selected evaluation month
- [ ] Successful runs write artifacts only into the active session output directory
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has a reusable frozen V5 run service that does not depend on the CLI entrypoint
- Later orchestration and UI plans can reuse one structured run-result contract instead of rebuilding execution logic
</success_criteria>

<output>
After completion, create `.planning/phases/03-v5-calculation-runs/03-01-SUMMARY.md`
</output>
