---
phase: 03-v5-calculation-runs
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - streamlit_app/ui/run_v5.py
  - streamlit_app/ui/wizard.py
  - tests/test_run_step_ui.py
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
user_setup: []
must_haves:
  truths:
    - Operators can review a suggested run month, override it, and start the frozen V5 calculation only from an explicit Run V5 action in the wizard.
    - After the run finishes, the run step stays in place and shows clear success or failure feedback plus the next action without forcing a fresh upload.
  artifacts:
    - streamlit_app/ui/run_v5.py
    - streamlit_app/ui/wizard.py
    - tests/test_run_step_ui.py
  key_links:
    - The wizard routes the `run-v5` step into a dedicated run UI module instead of leaving a placeholder.
    - The run UI reads the Phase 2 staged-input readiness payload and the Phase 3 run-status payload to drive controls, outcomes, and rerun behavior.
---

<objective>
Wire the real frozen V5 run experience into the wizard shell.

Purpose: Replace the Phase 1 placeholder with the Phase 3 operator workflow for selecting the run month, starting execution, and reading immediate success/failure outcomes.
Output: Dedicated run-step UI, explicit Run V5 interaction, same-step outcome summaries, and Streamlit AppTest coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-v5-calculation-runs/03-CONTEXT.md
@.planning/phases/03-v5-calculation-runs/03-RESEARCH.md
@.planning/phases/03-v5-calculation-runs/03-01-SUMMARY.md
@.planning/phases/03-v5-calculation-runs/03-02-SUMMARY.md
@streamlit_app/ui/wizard.py
@streamlit_app/services/run_workflow.py
@streamlit_app/ui/upload_inputs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the run-step controls with suggested-month override and explicit start</name>
  <files>streamlit_app/ui/run_v5.py, streamlit_app/ui/wizard.py, tests/test_run_step_ui.py</files>
  <action>Replace the `run-v5` placeholder with a dedicated UI module that shows a suggested evaluation month from the staged stock data, allows override, and exposes one explicit `Run V5` action. Keep the control surface lean and anchored to the staged inputs rather than adding advanced configuration, and add initial AppTest coverage in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_run_step_ui.py -k month</automated>
  </verify>
  <done>The run step is no longer a placeholder; operators can review the suggested month, override it, and explicitly start the frozen V5 run from the wizard shell.</done>
</task>

<task type="auto">
  <name>Task 2: Surface same-step success/failure outcomes and rerun guidance</name>
  <files>streamlit_app/ui/run_v5.py, tests/test_run_step_ui.py</files>
  <action>Extend the run UI so it stays on the same step after completion and shows clear success or failure summaries plus the next action. Reuse the orchestration payload for failure details, rerun messaging, and artifact readiness handoff to later phases without turning this step into a full review/export UI.</action>
  <verify>
    <automated>pytest -q tests/test_run_step_ui.py -k outcome</automated>
  </verify>
  <done>The operator can tell whether the run succeeded or failed, what to do next, and whether rerun can use the current staged files without leaving the run step.</done>
</task>

<task type="auto">
  <name>Task 3: Add AppTest coverage for run initiation, outcomes, and rerun behavior</name>
  <files>tests/test_run_step_ui.py</files>
  <action>Expand Streamlit AppTest coverage so it protects month suggestion display, explicit run initiation, success/failure summaries, and rerun behavior from the same session. Keep fixtures lightweight and centered on Phase 3 run-step behavior rather than Phase 4 result browsing.</action>
  <verify>
    <automated>pytest -q tests/test_run_step_ui.py</automated>
  </verify>
  <done>Phase 3 has browser-level coverage for the run step and can hand off a trustworthy completed-run state to Phase 4.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_run_step_ui.py` passes
- [ ] The run step requires an explicit Run V5 action
- [ ] Success and failure outcomes remain visible on the run step with clear next actions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The browser workflow can start the frozen V5 run from the wizard and explain what happened immediately after execution
- Phase 4 receives a completed-run state and artifact handoff instead of a placeholder run step
</success_criteria>

<output>
After completion, create `.planning/phases/03-v5-calculation-runs/03-03-SUMMARY.md`
</output>
