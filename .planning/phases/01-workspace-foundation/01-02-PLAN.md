---
phase: 01-workspace-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - streamlit_app/runtime/temp_workspace.py
  - streamlit_app/runtime/cleanup.py
  - streamlit_app/runtime/session_state.py
  - tests/test_temp_workspace.py
autonomous: true
requirements:
  - OPS-02
user_setup: []
must_haves:
  truths:
    - Each app session can allocate an isolated temporary workspace for uploaded inputs and generated outputs.
    - The foundation includes cleanup utilities that support both session-end cleanup and stale-artifact cleanup.
  artifacts:
    - streamlit_app/runtime/temp_workspace.py
    - streamlit_app/runtime/cleanup.py
    - tests/test_temp_workspace.py
  key_links:
    - session state resolves to a per-session workspace path
    - tests/test_temp_workspace.py verifies workspace creation and cleanup behavior
---

<objective>
Implement the temporary workspace lifecycle required for a no-storage MVP.

Purpose: Make the stateless design real by giving each session a safe temp area and by preventing abandoned files from accumulating.
Output: Temp workspace utilities, cleanup helpers, and automated coverage for the runtime primitives.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-foundation/01-RESEARCH.md
@.planning/phases/01-workspace-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement session-scoped temp workspace allocation</name>
  <files>streamlit_app/runtime/temp_workspace.py, streamlit_app/runtime/session_state.py, tests/test_temp_workspace.py</files>
  <action>Create runtime helpers that allocate deterministic, isolated workspace roots for each Streamlit session. Separate input staging from output artifacts so later upload and export phases can use the structure cleanly. Add the initial unit test cases for workspace creation in the same task so the verification command proves the new contract immediately. The implementation must avoid cross-session collisions and must not assume only one operator session exists.</action>
  <verify>
    <automated>pytest -q tests/test_temp_workspace.py -k create</automated>
  </verify>
  <done>Runtime utilities can create an isolated workspace for a session and expose the paths needed by later phases.</done>
</task>

<task type="auto">
  <name>Task 2: Add cleanup utilities for session-end and stale artifact removal</name>
  <files>streamlit_app/runtime/cleanup.py, streamlit_app/runtime/temp_workspace.py, tests/test_temp_workspace.py</files>
  <action>Implement cleanup helpers that support both immediate cleanup for an active session and age-based cleanup for abandoned workspaces. Extend the temp workspace test module with cleanup-focused cases in the same task so the cleanup verification is real. Keep the logic reusable so deployment scripts can call the same cleanup path later. Do not add persistent storage or retention logic beyond the stateless MVP boundary.</action>
  <verify>
    <automated>pytest -q tests/test_temp_workspace.py -k cleanup</automated>
  </verify>
  <done>Cleanup helpers exist for per-session teardown and stale workspace pruning without widening scope into storage features.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit coverage for the temp workspace contract</name>
  <files>tests/test_temp_workspace.py</files>
  <action>Add focused tests that prove workspace creation, path separation, and cleanup behavior. Keep the tests fast and filesystem-local so execute-phase can run them after every task commit. Cover the cases that would break the stateless MVP foundation most directly.</action>
  <verify>
    <automated>pytest -q tests/test_temp_workspace.py</automated>
  </verify>
  <done>Automated tests validate the temp workspace contract and can be reused as fast feedback in later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_temp_workspace.py` passes
- [ ] Temp workspaces are isolated per session
- [ ] Cleanup utilities support both immediate and stale-artifact cleanup
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app foundation has reusable temp workspace primitives for later upload/run phases
- Cleanup behavior supports the no-storage promise of the MVP
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-foundation/01-02-SUMMARY.md`
</output>
