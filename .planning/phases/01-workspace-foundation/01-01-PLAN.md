---
phase: 01-workspace-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pytest.ini
  - streamlit_app/__init__.py
  - streamlit_app/app.py
  - streamlit_app/ui/wizard.py
  - streamlit_app/runtime/session_state.py
  - streamlit_app/services/v5_boundary.py
  - tests/test_app_smoke.py
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - A Streamlit app entrypoint exists and boots into a step-by-step operator shell without crashing.
    - The foundation exposes a reusable boundary module for later V5 workflow integration instead of relying on CLI-only coupling.
  artifacts:
    - requirements.txt
    - streamlit_app/app.py
    - streamlit_app/ui/wizard.py
    - streamlit_app/services/v5_boundary.py
    - tests/test_app_smoke.py
  key_links:
    - streamlit_app/app.py imports the wizard shell and session bootstrap modules
    - tests/test_app_smoke.py exercises the app entrypoint
---

<objective>
Create the initial Streamlit application shell and the reusable V5 integration boundary for the stateless MVP foundation.

Purpose: Establish a real app runtime that later phases can extend without rewriting V5 or introducing a second execution path.
Output: A bootable Streamlit app package, placeholder wizard shell, dependency manifest, and smoke coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-foundation/01-RESEARCH.md
@v5_daily_oos_opportunity/main.py
@v5_daily_oos_opportunity/data_loader_v5.py
@v5_daily_oos_opportunity/analyzer_v5.py
@v5_daily_oos_opportunity/reporter_v5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the Streamlit app package and dependency baseline</name>
  <files>requirements.txt, pytest.ini, streamlit_app/__init__.py, streamlit_app/app.py</files>
  <action>Create the first real app runtime for the repo. Add a dependency manifest for Streamlit and pytest-based validation, then create a `streamlit_app` package with a single entrypoint module. Keep the structure intentionally small and Python-first; do not introduce a separate backend framework or storage layer in this plan. The entrypoint must be importable and ready for later phases to extend.</action>
  <verify>
    <automated>python3 -m compileall streamlit_app</automated>
  </verify>
  <done>`streamlit_app/app.py` exists, imports cleanly, and the repo has an explicit dependency baseline for Phase 1 work.</done>
</task>

<task type="auto">
  <name>Task 2: Build the guided wizard shell and session bootstrap seam</name>
  <files>streamlit_app/app.py, streamlit_app/ui/wizard.py, streamlit_app/runtime/session_state.py</files>
  <action>Implement the operator-facing shell as a step-by-step wizard, matching the decisions captured in `01-CONTEXT.md`. The shell should render a real Phase 1 foundation experience, not a blank page: initialize session state, show the MVP boundary, and reserve step placeholders for later upload/run/result phases without implementing those workflows yet. Keep the shell modular so later plans can extend it cleanly.</action>
  <verify>
    <automated>python3 -m compileall streamlit_app</automated>
  </verify>
  <done>The app opens into a guided shell with initialized session state and clear placeholder steps for the stateless MVP flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add the V5 boundary module and smoke coverage</name>
  <files>streamlit_app/services/v5_boundary.py, tests/test_app_smoke.py</files>
  <action>Create a boundary module that centralizes how the app will talk to the frozen V5 pipeline in later phases. Reuse existing V5 modules rather than inventing a second logic path. Add a smoke test that proves the app entrypoint loads and the shell renders without crashing. Keep the boundary light in this phase; it should define the seam, not implement the full run workflow yet.</action>
  <verify>
    <automated>pytest -q tests/test_app_smoke.py</automated>
  </verify>
  <done>A reusable V5 boundary module exists and automated smoke coverage proves the new app shell boots successfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m compileall streamlit_app` passes
- [ ] `pytest -q tests/test_app_smoke.py` passes
- [ ] The Streamlit shell opens into a guided foundation flow without implementing out-of-scope storage features
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a real Streamlit app entrypoint and dependency baseline
- The app shell reflects the Phase 1 decisions from context
- Later phases have a clear module boundary for integrating V5 logic
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-foundation/01-01-SUMMARY.md`
</output>
