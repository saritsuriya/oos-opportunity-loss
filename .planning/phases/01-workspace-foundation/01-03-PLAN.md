---
phase: 01-workspace-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .streamlit/config.toml
  - scripts/run_streamlit.ps1
  - scripts/cleanup_temp_workspace.py
  - scripts/cleanup_temp_workspace.ps1
  - docs/phase-01-deployment.md
  - tests/test_deploy_config.py
autonomous: true
requirements:
  - OPS-01
user_setup:
  - "Choose the final Windows server host/port and run location for the internal deployment."
  - "Create the Windows startup/service and scheduled cleanup task using the generated scripts or equivalent host tooling."
must_haves:
  truths:
    - The app has a documented, repeatable startup path for internal browser access on the Windows server.
    - Deployment artifacts exist for both app startup and temp-workspace cleanup operations.
  artifacts:
    - .streamlit/config.toml
    - scripts/run_streamlit.ps1
    - scripts/cleanup_temp_workspace.py
    - scripts/cleanup_temp_workspace.ps1
    - docs/phase-01-deployment.md
    - tests/test_deploy_config.py
  key_links:
    - The deployment runbook references the startup and cleanup scripts directly
    - The startup script targets the Streamlit app entrypoint created in Plan 01-01
---

<objective>
Create the internal deployment baseline for the Streamlit foundation on the chosen Windows-hosted environment.

Purpose: Turn the new app shell into something the team can actually run and reach through a normal internal browser URL.
Output: Streamlit config, Windows-facing startup and cleanup scripts, deployment documentation, and deploy-config smoke coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-foundation/01-RESEARCH.md
@.planning/phases/01-workspace-foundation/01-01-SUMMARY.md
@.planning/phases/01-workspace-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Streamlit runtime configuration for internal hosting</name>
  <files>.streamlit/config.toml, tests/test_deploy_config.py</files>
  <action>Create the baseline Streamlit configuration needed for internal hosting and current file sizes. Keep the config aligned with the research: headless server runtime, explicit upload-size posture, and settings that support a Windows-hosted internal URL. Create the initial deployment smoke test module in the same task so the config verification runs against a real test file immediately.</action>
  <verify>
    <automated>pytest -q tests/test_deploy_config.py -k config</automated>
  </verify>
  <done>The repo contains a concrete Streamlit config file and automated checks for the deployment baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Create startup and cleanup scripts for the Windows deployment path</name>
  <files>scripts/run_streamlit.ps1, scripts/cleanup_temp_workspace.py, scripts/cleanup_temp_workspace.ps1, tests/test_deploy_config.py</files>
  <action>Create the operational scripts needed to start the Streamlit app and run stale-workspace cleanup from the Windows server. Reuse the runtime cleanup logic from Plan 01-02 rather than duplicating behavior, and extend the deployment smoke test module with script assertions in the same task. The scripts should be explicit enough that the user can turn them into a service and scheduled task later without needing extra design work.</action>
  <verify>
    <automated>pytest -q tests/test_deploy_config.py -k scripts</automated>
  </verify>
  <done>Operational scripts exist for app startup and cleanup, and they align with the stateless MVP runtime contract.</done>
</task>

<task type="auto">
  <name>Task 3: Write the operator deployment runbook</name>
  <files>docs/phase-01-deployment.md, tests/test_deploy_config.py</files>
  <action>Document the exact local-to-Windows deployment baseline for this phase: dependencies, startup command, expected internal URL shape, bundled site-mapping behavior, cleanup scheduling intent, and the manual setup items the user must complete on the target host. Keep the runbook specific to Phase 1 and do not drift into persistent storage or production-hardening work from later phases.</action>
  <verify>
    <automated>pytest -q tests/test_deploy_config.py</automated>
  </verify>
  <done>The repo contains a concrete deployment/runbook artifact that makes the Phase 1 foundation operable on the chosen internal host.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_deploy_config.py` passes
- [ ] Startup and cleanup scripts exist and reference real project artifacts
- [ ] The runbook describes a repeatable internal deployment path for the Windows server
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The project has a documented and script-backed internal deployment baseline
- Phase 1 can be reached via a normal internal browser URL once the user completes host-specific setup
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-foundation/01-03-SUMMARY.md`
</output>
