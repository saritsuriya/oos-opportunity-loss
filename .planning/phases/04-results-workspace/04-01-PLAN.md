---
phase: 04-results-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - streamlit_app/services/results_workspace.py
  - tests/test_results_workspace.py
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
user_setup: []
must_haves:
  truths:
    - The app can reconstruct a trustworthy browser-facing results model from the completed Phase 3 artifact files without rerunning frozen V5.
    - CSV-backed result tables and workbook-only trust aids are available through one structured service contract that later UI work can reuse.
  artifacts:
    - streamlit_app/services/results_workspace.py
    - tests/test_results_workspace.py
  key_links:
    - The results service reads the completed-run payload from Phase 3 and validates the referenced artifact files under the active session workspace.
    - The service prefers CSV-backed summaries/detail/QA and only reads workbook-only sheets for unmapped sites, definitions, and the calculation example.
---

<objective>
Create the reusable result-loading seam for the browser review step.

Purpose: Give Phase 4 a file-based service contract that can load a completed run's summaries, detail rows, QA signals, explainability helpers, and export manifest without invoking the calculation again.
Output: Structured results loader, artifact validation, workbook-only trust-sheet access, and focused unit coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-workspace/04-CONTEXT.md
@.planning/phases/04-results-workspace/04-RESEARCH.md
@streamlit_app/services/run_workflow.py
@streamlit_app/services/run_execution.py
@streamlit_app/services/v5_boundary.py
@v5_daily_oos_opportunity/reporter_v5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the completed-run artifact manifest and CSV-backed results loader</name>
  <files>streamlit_app/services/results_workspace.py, tests/test_results_workspace.py</files>
  <action>Create the Phase 4 service contract that reads the completed-run payload from session state, validates artifact paths, and loads the browser-facing result tables from the generated CSV files. Include overview metrics, summary tables, detail rows, QA summary, and a structured export manifest, and add initial unit tests in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_results_workspace.py -k artifact</automated>
  </verify>
  <done>The app can load the main Phase 4 browser tables and overview metrics from a completed run without reopening the calculation path.</done>
</task>

<task type="auto">
  <name>Task 2: Add workbook-only trust-sheet loading for unmapped sites, definitions, and calculation example</name>
  <files>streamlit_app/services/results_workspace.py, tests/test_results_workspace.py</files>
  <action>Extend the service to load the workbook-only sheets that Phase 4 needs for trust and explainability: unmapped-site rows, definitions, and the calculation example. Keep those reads lazy-friendly and structured for later UI tabs, and add focused tests in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_results_workspace.py -k workbook</automated>
  </verify>
  <done>Phase 4 has one service contract that covers both CSV-backed browser tables and workbook-only trust aids.</done>
</task>

<task type="auto">
  <name>Task 3: Harden missing-artifact and invalid-run failure handling</name>
  <files>streamlit_app/services/results_workspace.py, tests/test_results_workspace.py</files>
  <action>Expand service coverage so it fails cleanly when the run payload is missing, artifact files have been cleaned up, or workbook trust sheets are unavailable. Keep the errors structured and operator-safe so the review step can explain what went wrong without rerunning the model automatically.</action>
  <verify>
    <automated>pytest -q tests/test_results_workspace.py</automated>
  </verify>
  <done>Fast unit coverage protects the browser results loader against stale, incomplete, or missing run artifacts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_results_workspace.py` passes
- [ ] The service loads summary, detail, QA, and export metadata from the completed-run artifacts
- [ ] Missing or invalid artifacts return structured errors instead of silent failures
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has a reusable results-loading seam that reuses Phase 3 artifacts instead of recalculating them
- Later UI plans can render overview, detail, QA, and export from one structured service contract
</success_criteria>

<output>
After completion, create `.planning/phases/04-results-workspace/04-01-SUMMARY.md`
</output>
