---
phase: 04-results-workspace
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - streamlit_app/ui/review_results.py
  - tests/test_review_results_ui.py
autonomous: true
requirements:
  - RES-02
  - RES-04
  - RES-05
user_setup: []
must_haves:
  truths:
    - Operators can download the generated workbook and named CSV files from a dedicated export tab without regenerating results.
    - The review step handles missing or stale artifacts clearly and tells the operator when they need to rerun before trusting exports.
  artifacts:
    - streamlit_app/ui/review_results.py
    - tests/test_review_results_ui.py
  key_links:
    - The export tab consumes the existing artifact manifest from the Phase 4 results service rather than building a second output path.
    - Review-step failure handling reuses the Phase 3 run-state contract so stale or missing outputs are explained consistently.
---

<objective>
Finish the review step with export actions and final resilience polish.

Purpose: Turn the review workspace into a complete operational handoff by exposing workbook/CSV downloads and handling missing-artifact or stale-result cases clearly.
Output: Dedicated export tab, download actions, artifact-failure handling, and final AppTest protection.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-workspace/04-CONTEXT.md
@.planning/phases/04-results-workspace/04-RESEARCH.md
@.planning/phases/04-results-workspace/04-02-PLAN.md
@streamlit_app/ui/review_results.py
@streamlit_app/services/results_workspace.py
@streamlit_app/services/run_workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated export tab with workbook-first and named CSV downloads</name>
  <files>streamlit_app/ui/review_results.py, tests/test_review_results_ui.py</files>
  <action>Implement the export tab inside the review workspace. Surface one prominent workbook download plus individual CSV download actions for the generated artifacts, and include lightweight filename/type context for each action. Add focused AppTest coverage in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_review_results_ui.py -k export</automated>
  </verify>
  <done>The browser review step exposes the expected workbook and CSV handoff from the completed run artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Handle missing-artifact and invalid-review cases with clear operator guidance</name>
  <files>streamlit_app/ui/review_results.py, tests/test_review_results_ui.py</files>
  <action>Extend the review UI so it fails cleanly when result files are missing, the run payload is incomplete, or the current run is no longer trustworthy for review. Keep the messages operator-safe and action-oriented, and add focused tests in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_review_results_ui.py -k missing</automated>
  </verify>
  <done>Operators see a clear explanation and next action when the review step cannot safely show or export results.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the full review/export workflow with final browser-level coverage</name>
  <files>tests/test_review_results_ui.py</files>
  <action>Expand AppTest coverage so it protects the finished results step end to end: overview, detail, QA/trust, export actions, and missing-artifact paths. Keep the suite fast and centered on Phase 4 behavior rather than re-executing the calculation flow.</action>
  <verify>
    <automated>pytest -q tests/test_review_results_ui.py</automated>
  </verify>
  <done>The results workspace is protected by browser-level tests for the full review-and-export operator workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_review_results_ui.py` passes
- [ ] The export tab offers the workbook and named CSV downloads from the completed run artifacts
- [ ] Missing or stale review cases show clear operator guidance instead of silent or broken exports
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The browser results workspace can hand off workbook and CSV exports directly from the completed run artifacts
- The step remains reliable when artifacts are missing or no longer current for review
</success_criteria>

<output>
After completion, create `.planning/phases/04-results-workspace/04-03-SUMMARY.md`
</output>
