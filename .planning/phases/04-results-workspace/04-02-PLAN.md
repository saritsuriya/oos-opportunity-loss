---
phase: 04-results-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - streamlit_app/ui/review_results.py
  - streamlit_app/ui/wizard.py
  - tests/test_review_results_ui.py
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
user_setup: []
must_haves:
  truths:
    - The `review-results` step becomes an overview-first browser workspace with one-page tabs for summaries, detail browsing, and QA/trust review.
    - The browser detail experience stays read-only and lightweight while surfacing the key explainability fields needed to understand why loss exists.
  artifacts:
    - streamlit_app/ui/review_results.py
    - streamlit_app/ui/wizard.py
    - tests/test_review_results_ui.py
  key_links:
    - The review UI reads the Phase 4 results service instead of loading files directly inside widgets.
    - The wizard routes the `review-results` step into the dedicated UI module and preserves the Phase 3 current-run freshness contract.
---

<objective>
Build the browser review workspace for results, QA, and explainability.

Purpose: Replace the Phase 1 placeholder with the real Phase 4 operator experience for reading summary outputs, inspecting detail rows, and understanding QA/trust signals before export.
Output: Overview-first review UI, tabbed summary/detail/QA surfaces, and Streamlit AppTest coverage.
</objective>

<execution_context>
@/Users/saritsuriyasangpetch/.codex/get-shit-done/workflows/execute-plan.md
@/Users/saritsuriyasangpetch/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-workspace/04-CONTEXT.md
@.planning/phases/04-results-workspace/04-RESEARCH.md
@.planning/phases/04-results-workspace/04-01-PLAN.md
@streamlit_app/ui/wizard.py
@streamlit_app/ui/run_v5.py
@streamlit_app/services/run_workflow.py
@streamlit_app/services/results_workspace.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the review-step placeholder with the overview-first results workspace</name>
  <files>streamlit_app/ui/review_results.py, streamlit_app/ui/wizard.py, tests/test_review_results_ui.py</files>
  <action>Create the dedicated Phase 4 review UI module and route the wizard's `review-results` step into it. Start with the overview block that shows headline totals, completed-run context, and the visible QA/trust signal above the tabbed review surfaces, and add initial AppTest coverage in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_review_results_ui.py -k overview</automated>
  </verify>
  <done>The review step is no longer a placeholder; operators land on an overview-first results workspace after a successful run.</done>
</task>

<task type="auto">
  <name>Task 2: Add site, SKU, and detail tabs with browser-friendly explainability</name>
  <files>streamlit_app/ui/review_results.py, tests/test_review_results_ui.py</files>
  <action>Render the summary-by-site, summary-by-SKU, and detail tabs from the results service. Keep the detail table read-only and lightweight while surfacing the locked explainability fields such as baseline source, baseline window, recorded days, OOS days, and actual quantity, and add focused AppTest coverage in the same task.</action>
  <verify>
    <automated>pytest -q tests/test_review_results_ui.py -k detail</automated>
  </verify>
  <done>Operators can browse summary tables and inspect row-level result logic in the browser before exporting.</done>
</task>

<task type="auto">
  <name>Task 3: Build the QA/trust tab with warnings, unmapped rows, definitions, and calculation example</name>
  <files>streamlit_app/ui/review_results.py, tests/test_review_results_ui.py</files>
  <action>Implement the dedicated QA/trust tab that surfaces the QA summary, unmapped site rows, definitions, and calculation example from the results service. Keep the trust posture visible but non-blocking and add AppTest coverage that protects the tab content and messaging.</action>
  <verify>
    <automated>pytest -q tests/test_review_results_ui.py -k qa</automated>
  </verify>
  <done>The browser workspace makes QA signals and explainability aids easy to review without adding a separate approval workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_review_results_ui.py` passes for overview, detail, and QA flows
- [ ] The overview leads with totals and a visible QA/trust signal
- [ ] The detail browser exposes the key explainability fields in the on-screen table
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The browser results workspace supports summary review, row-level inspection, and trust review within one step
- Phase 4 remains lean and read-only instead of turning into a dashboard or analyst workbench
</success_criteria>

<output>
After completion, create `.planning/phases/04-results-workspace/04-02-SUMMARY.md`
</output>
